(() => {
  function insertionSort(arr) {
    for (let i = 1; i < arr.length; i++) {
      for (let j = 0; j < i; j++) {
        if (arr[j] < arr[i]) {
          //
        }
      }
    }
  }
})();

// this is called and iify(immediately invoked function expression)
